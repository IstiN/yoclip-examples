name: dmtools_promo
description: DMTools — an EPAM open-source promo film built with Yoclip.
publish_to: none

version: 0.1.0+1

environment:
  sdk: ^3.12.0
  flutter: ^3.44.0

dependencies:
  flutter:
    sdk: flutter
  integration_test:
    sdk: flutter
  path: ^1.9.0
  yoclip_core:
    path: ../../../packages/yoclip_core
  yoclip_encoder:
    path: ../../../packages/yoclip_encoder

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - yoclip.yaml
